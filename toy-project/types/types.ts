export interface ProfileType {
  login: string;
  avatar_url: string;
  bio: string;
  followers: number;
  following: number;
}

export interface followList {
  login: string;
  avatar_url: string;
}
